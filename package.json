{
  "name": "goodreads-api",
  "version": "0.0.0",
  "description": "An API wrapper for Goodreads for Node.js",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "yarn build && nodemon",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublish": "tsc"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "nodemon": "^2.0.4",
    "typescript": "^3.9.5"
  },
  "dependencies": {
    "@types/xml2js": "^0.4.5",
    "axios": "^0.19.2",
    "xml2js": "^0.4.23"
  }
}
